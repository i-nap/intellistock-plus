# Accessibility Rules

- Use semantic HTML.
- Buttons must be buttons, not clickable divs.
- Inputs need labels.
- Dialogs must be keyboard accessible.
- Keep good color contrast.
- Do not rely only on color to show status.
- Add helpful aria labels where needed.
