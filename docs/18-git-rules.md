# Git Rules

Use clear commit messages.

Recommended format:

```txt
feat(inventory): add stock adjustment form
fix(auth): handle invalid login response
refactor(products): move product constants
test(reorder): add reorder calculation tests
```

Keep PRs focused. Avoid mixing unrelated frontend, backend, and formatting changes in one PR.
